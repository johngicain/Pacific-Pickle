
<script
  src="{{ 'blog-posts-list.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

{% liquid
  if section.settings.background_color != blank
    render 'contrast-override', background_color: section.settings.background_color, section_id: section.id
  endif

  assign show_featured = section.settings.show_featured
  if current_tags != blank
    assign show_featured = false
  endif
%}

<blog-posts-list section-id="{{ section.id }}">
  <div
    class="section-background{% if section.settings.background_color != blank %} color-custom-{{ section.id }}{% endif %}"
  ></div>
  <div
    class="
      section
      section--{{ section.settings.section_width }}
      {% if section.settings.background_color != blank %}color-custom-{{ section.id }}{% endif %}
      blog-posts
      spacing-style
      size-style
      {% if show_featured %}blog-posts--featured{% endif %}
    "
    data-testid="blog-posts"
    style="{% render 'spacing-style', settings: section.settings %}"
  >
    {% content_for 'blocks' %}

    <div
      ref="grid"
      class="blog-posts-container"
    >
      {% for article in blog.articles %}
        {% liquid
          assign is_featured = false
          if show_featured and forloop.first
            assign is_featured = true
          endif
        %}
        <div
          class="blog-post-item{% if is_featured %} blog-post-item--horizontal blog-post-item--featured{% endif %}"
          data-testid="blog-post-item"
          data-blog-index="{{ section.id }}-{{ forloop.index }}"
          data-category="{{ article.metafields.custom.category.value | handleize }}"
        >
          {% content_for 'block',
            id: 'static-blog-post-card',
            type: '_featured-blog-posts-card',
            article: article,
            closest.article: article
          %}
        </div>
      {% else %}
        <div class="blog-posts-empty">
          <p>No recipes found.</p>
        </div>
      {% endfor %}
    </div>
  </div>
</blog-posts-list>

{% stylesheet %}
  .blog-posts {
    --page-content-width: var(--normal-page-width);
    --page-width: calc(var(--page-content-width) + (var(--page-margin) * 2));
    --columns-gap: 24px;
    --rows-gap: 24px;
  }

  .blog-posts-container {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 1rem;
    width: 100%;
    column-gap: var(--columns-gap);
    row-gap: var(--rows-gap);
  }

  .blog-post-item {
    grid-column: span 3;
    min-width: 0;
  }

  .blog-post-item--featured {
    grid-column: span 6;
  }

  @media screen and (max-width: 749px) {
    .blog-post-item,
    .blog-post-item--featured {
      grid-column: span 6;
    }
  }

  .blog-post-item--horizontal .featured-blog-posts-card {
    height: 100%;
  }

  .blog-post-item--horizontal .featured-blog-posts-card__inner {
    flex-direction: row;
    min-height: 22rem;
  }

  .blog-post-item--horizontal .featured-blog-posts-card__image {
    flex: 1 1 50%;
    min-width: 0;
    height: auto;
    align-self: stretch;
  }

  .blog-post-item--horizontal .featured-blog-posts-card__image .resource-image__image,
  .blog-post-item--horizontal .featured-blog-posts-card__image .blog-placeholder-svg {
    height: 100%;
    aspect-ratio: unset;
    object-fit: cover;
  }

  .blog-post-item--horizontal .featured-blog-posts-card__inner > .group-block {
    flex: 1 1 50%;
    min-width: 0;
    justify-content: center;
  }

  .blog-post-item--featured {
    position: relative;
  }

  .blog-post-item--featured::before {
    content: '✦ Featured Recipe';
    position: absolute;
    top: 1rem;
    left: 1rem;
    z-index: 2;
    background: #e8821a;
    color: #fff;
    font-family: var(--font-body--family);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.4rem 0.75rem;
    pointer-events: none;
  }

  .blog-post-item--featured .featured-blog-posts-card {
    background-color: #1c1c1a;
    border-color: #1c1c1a;
  }

  .blog-post-item--featured .featured-blog-posts-card__inner > .group-block {
    background-color: #1c1c1a;
    color: #ffffff;
    --color-foreground: #ffffff;
    --color-foreground-rgb: 255 255 255;
    padding: 2rem !important;
  }

  .blog-post-item--featured .featured-blog-posts-card .text-block:is(.h1, .h2, .h3, .h4, .h5, .h6),
  .blog-post-item--featured .featured-blog-posts-card .text-block:is(.h1, .h2, .h3, .h4, .h5, .h6) > * {
    color: #ffffff !important;
    white-space: normal;
    overflow: visible;
    text-overflow: unset;
    -webkit-line-clamp: unset;
    line-clamp: unset;
    font-size: clamp(1.75rem, 3vw, 2.75rem);
    line-height: 1.05;
  }

  .blog-post-item--featured .blog-post-card__content-text,
  .blog-post-item--featured .blog-post-card__content-text * {
    color: #ffffff !important;
  }

  .blog-post-item--featured .button-unstyled {
    color: #e8821a !important;
  }

  .blog-posts-empty {
    grid-column: 1 / -1;
    text-align: center;
    padding: 3rem 1rem;
  }

  .blog-post-item--featured .text-block :is(h1, h2, h3, h4, h5, h6),
  .blog-post-item--featured .text-block > * {
    white-space: normal;
    text-overflow: unset;
    overflow: visible;
    -webkit-line-clamp: unset;
    line-clamp: unset;
  }

  @media screen and (max-width: 749px) {
    .blog-post-item--horizontal .featured-blog-posts-card__inner {
      flex-direction: column;
      min-height: 0;
    }

    .blog-post-item--horizontal .featured-blog-posts-card__inner > .group-block {
      padding-block: 1.25rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.blog_posts",
  "blocks": [
    {
      "type": "@theme"
    },
    {
      "type": "@app"
    },
    {
      "type": "text"
    },
    {
      "type": "icon"
    },
    {
      "type": "image"
    },
    {
      "type": "button"
    },
    {
      "type": "video"
    },
    {
      "type": "group"
    },
    {
      "type": "spacer"
    },
    {
      "type": "_divider"
    },
    {
      "type": "_featured-blog-posts-card"
    },
    {
      "type": "_featured-blog-posts-image"
    },
    {
      "type": "custom-liquid"
    }
  ],
  "disabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "show_featured",
      "label": "Show featured recipe",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "link_unstyled_buttons_to_article",
      "label": "t:settings.link_unstyled_buttons_to_article",
      "info": "t:info.link_unstyled_buttons_to_article",
      "default": true
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "t:settings.background_color",
      "placeholder": "t:settings.default"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ]
}
{% endschema %}
